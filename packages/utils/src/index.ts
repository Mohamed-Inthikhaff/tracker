/**
 * Shared pure utilities (currency formatting, date math, etc.).
 * Scaffold only — real helpers land when a second consumer needs them.
 */
export function identity<T>(value: T): T {
  return value;
}
